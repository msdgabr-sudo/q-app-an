name: RETIRED - q-app-an Pages deployment

# Production ownership of app.qiblalabs.com moved to msdgabr-sudo/Fog.
# This repository is retained only as a rollback/history source for Code 3.
# IMPORTANT: this workflow intentionally performs no deployment and must not
# reclaim the custom domain or publish the old Code 3 web surface.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm retired publishing source
        run: |
          echo "q-app-an Pages deployment is retired. Production is published from msdgabr-sudo/Fog."
